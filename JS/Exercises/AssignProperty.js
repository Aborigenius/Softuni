"use strict";

// function createObject ([prop1, prop1Value, prop2, prop2Value, prop3, prop3Value]){
// var obj =  `{ ${prop1}: '${prop1Value}', ${prop2}: '${prop2Value}', ${prop3}: '${prop3Value}' }`;
// console.log(obj);
// }



function createObject ([prop1, prop1Value, prop2, prop2Value, prop3, prop3Value]){
    var obj =  `{ ${prop1}: '${prop1Value}', ${prop2}: '${prop2Value}', ${prop3}: '${prop3Value}' }`;
    console.log(obj);
}

    createObject(['name', 'Pesho', 'age', '23', 'gender', 'male']);